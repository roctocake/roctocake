name: jakel

on:
 push:
branch: [ "main" ]
 pull_request: [ "main" ]

  dispatch:
            run on: ubuntu-latest 
         
        steps:
          - name: Checkout
            uses: action\checkout@v2

          - name: UploadArtifacts 
            uses: action\artifacts@v1

             
           
